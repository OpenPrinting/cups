<H2 CLASS="title">{is_class?打印机组:打印机} {printer_name} 设为默认</H2>

<P>{is_class?打印机组:打印机} <A
HREF="/{is_class?classes:printers}/{printer_name}">{printer_name}</A>
已设为服务器默认打印机。</P>

<BLOCKQUOTE><B>注意:</B> <TT>lpoptions</TT> 命令设置的各用户默认会覆盖此服务器默认设置。</BLOCKQUOTE>
